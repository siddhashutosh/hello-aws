{
  "name": "hello-aws",
  "version": "1.0.0",
  "private": true,
  "description": "Hello-world service on the AWS Always Free tier, with a dev -> test -> prod promotion pipeline",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/local.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
