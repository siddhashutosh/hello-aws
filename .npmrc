# Fail fast on the wrong Node major rather than shipping a zip built against a
# runtime the Lambda does not have. `engines` in package.json is advisory
# without this.
engine-strict=true
save-exact=true
fund=false
audit=false
